# ==========================================
# DATABASE CONFIGURATION
# ==========================================

# Database client: 'postgres' for production, 'sqlite' for dev
DATABASE_CLIENT=postgres

# PostgreSQL connection (used when DATABASE_CLIENT=postgres)
DATABASE_HOST=postgres
DATABASE_PORT=5432
DB_NAME=strapi
DB_USER=strapi
DB_PASSWORD=strapi

# ⚠️  IMPORTANT for Production:
# - Change DB_PASSWORD to a strong password
# - Keep passwords SHORT (avoid line wrapping in Docker)
# - Recommended: Use 16-32 character alphanumeric passwords

# PostgreSQL credentials for backup script
POSTGRES_USER=strapi
POSTGRES_PASSWORD=strapi
POSTGRES_DB=strapi

# ==========================================
# STRAPI SECRETS
# ==========================================
# Generate secure secrets with: openssl rand -base64 32
# Or use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

APP_KEYS=GENERATE_RANDOM_STRING_HERE
API_TOKEN_SALT=GENERATE_RANDOM_STRING_HERE
ADMIN_JWT_SECRET=GENERATE_RANDOM_STRING_HERE
TRANSFER_TOKEN_SALT=GENERATE_RANDOM_STRING_HERE
JWT_SECRET=GENERATE_RANDOM_STRING_HERE

# ==========================================
# PUBLIC URLs
# ==========================================

# Backend API URL (used by frontend)
# Development: http://localhost:1337
# Production: https://api.yourdomain.com
PUBLIC_STRAPI_URL=http://localhost:1337

# ==========================================
# DEVELOPMENT NOTES
# ==========================================
# 
# For local development, you can use:
# - DB_PASSWORD=strapi (simple, short, no wrapping issues)
# - PUBLIC_STRAPI_URL=http://localhost:1337
#
# For production deployment:
# 1. Change DB_PASSWORD to strong password (16-32 chars recommended)
# 2. Update PUBLIC_STRAPI_URL to your production API domain
# 3. Regenerate all secrets (APP_KEYS, JWT_SECRET, etc.)
# 4. Never commit .env to git (already in .gitignore)
#
# Docker services:
# - PostgreSQL: localhost:5432
# - Backend API: localhost:1337
# - Backend Admin: localhost:1337/admin
# - Frontend: localhost:4321
# - Nginx: localhost:80, localhost:443
